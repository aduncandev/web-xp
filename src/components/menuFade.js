import { keyframes } from 'styled-components';

export const menuFade = keyframes`
  from { opacity: 0; }
  to { opacity: 1; }
`;

export const MENU_FADE_MS = 130;
export const SUBMENU_SHOW_DELAY_MS = 400;
